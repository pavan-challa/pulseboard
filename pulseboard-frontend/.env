# Copy to .env for local dev (Vite auto-loads .env files, gitignored by default).
# In Vercel, set this as a Project > Settings > Environment Variable instead,
# pointing at your deployed Railway backend URL.
VITE_API_BASE_URL=http://localhost:8080
